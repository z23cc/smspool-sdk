[package]
name = "smspool"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
description = "Async Rust SDK for the SMSPool API"
license = "MIT OR Apache-2.0"
readme = "README.md"
keywords = ["smspool", "sms", "otp", "api-client", "async"]
categories = ["api-bindings", "asynchronous"]
repository = "https://github.com/z23cc/smspool-sdk"
# `documentation` is intentionally unset: crates.io defaults it to docs.rs,
# which is the correct target once published.
autoexamples = false
exclude = [
    "/_git_data",
    "/prompt-exports",
    "/acceptance",
    "/contracts",
    "/scripts",
    "/docs",
    "/tests",
    "/postman.json",
]

[features]
default = ["rustls-tls"]
rustls-tls = ["reqwest/rustls-tls"]
tracing = ["dep:tracing"]
postgres-example = [
    "dep:axum",
    "dep:chacha20poly1305",
    "dep:hex",
    "dep:sha2",
    "dep:sqlx",
]

[dependencies]
futures-util = "=0.3.34"
http = "=1.5.0"
httpdate = "=1.0.3"
rand = "=0.9.3"
reqwest = { version = "=0.12.28", default-features = false, features = ["multipart", "stream"] }
rust_decimal = "=1.42.1"
secrecy = "=0.10.3"
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
serde_path_to_error = "=0.1.20"
thiserror = "=2.0.20"
tokio = { version = "=1.53.1", features = ["macros", "net", "rt", "signal", "sync", "time"] }
tokio-util = { version = "=0.7.19", features = ["rt"] }
tracing = { version = "=0.1.44", optional = true }
url = "=2.5.8"
axum = { version = "=0.8.9", optional = true }
chacha20poly1305 = { version = "=0.10.1", optional = true }
hex = { version = "=0.4.3", optional = true }
sha2 = { version = "=0.10.9", optional = true }
sqlx = { version = "=0.8.3", default-features = false, features = ["postgres", "runtime-tokio-rustls"], optional = true }

[dev-dependencies]
tokio = { version = "=1.53.1", features = ["io-util"] }
tracing-subscriber = "=0.3.23"

[[example]]
name = "axum"
required-features = ["postgres-example"]
